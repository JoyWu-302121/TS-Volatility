# Extensions

This directory isolates new, self-contained research extensions from the repository's legacy notebooks and canonical 30-minute realized-variance workflow.

Each extension owns its documentation, dependencies, code, scripts, and derived data. Large raw datasets belong only to the extension that uses them.

## Projects

- `spx_1min_volatility/`: a new SPX one-minute realized-volatility reconstruction and forecasting project.
